 aws ec2 describe-snapshots --owner-ids self --query 'Snapshots[?StartTime>`2023-09-30`]' | jq -r '(first | to_entries | map(.key)), (map([.Description, .Encrypted, .KmsKeyId, .OwnerId, .Progress, .SnapshotId, .StartTime, .State, .VolumeId, .VolumeSize, (.Tags | map([.Key, .Value] | join(": ")) | join("; "))]))[] | @csv' > snapshots.csv
